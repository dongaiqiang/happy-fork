# HV-006 2026-04-08 Trae-Main 账号提档能力收口验证结果

## 1. 验证目标

- 验证 `/admin/upgrade` 现有能力在本轮收口后仍可用
- 验证新补的最小内部脚本入口可直接执行提档
- 验证提档后的订阅状态、有效期与 quota 数值符合目标档位，不再停留在低档位额度

## 2. 验证命令与结果

### 2.1 路由测试

执行命令：

```bash
yarn workspace happy-server test sources/app/api/routes/quotaRoutes.test.ts
```

结果：

- 通过
- 共 6 条测试全部通过
- 覆盖：
  - `ADMIN_TOKEN` 未配置返回 `503`
  - token 缺失或错误返回 `401`
  - 按 `username` 提档成功
  - 按 `accountId` 提档成功
  - 提回 `free` 时删除订阅
  - 更新已有订阅时保留原始 `startDate`

### 2.2 类型与构建校验

执行命令：

```bash
yarn workspace happy-server build
```

结果：

- 通过
- `prisma generate` 完成
- `tsc --noEmit` 通过

### 2.3 真实脚本验证链路

执行位置：

- `/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/hellovibe-main/packages/happy-server`

验证步骤：

1. 通过 `DATABASE_URL` 连接本地数据库
2. 插入临时账号 `hv006-trae-main-01-account`
3. 执行：

```bash
yarn admin:upgrade --account-id hv006-trae-main-01-account --tier enterprise --billing-period annual
```

4. 查询 `SubscriptionPlan`
5. 清理临时账号与临时订阅记录

脚本实际返回关键结果：

```json
{
  "success": true,
  "account": {
    "id": "hv006-trae-main-01-account",
    "username": "hv006_trae_main_01"
  },
  "subscription": {
    "tier": "enterprise",
    "status": "active",
    "startDate": "2026-04-08T07:04:35.664Z",
    "endDate": "2027-04-08T07:04:35.664Z",
    "features": {
      "dailyLimit": 5000000,
      "monthlyLimit": 100000000,
      "rateLimit": 1000,
      "storageLimit": "1073741824000"
    }
  }
}
```

数据库查询结果：

```text
accountId                   | hv006-trae-main-01-account
tier                        | enterprise
status                      | active
dailyLimit                  | 5000000
tokensLimit                 | 100000000
endDate                     | 2027-04-08T07:04:35Z
```

验证结论：

- 临时账号已被成功提到 `enterprise`
- `dailyLimit` 已从低档位量级提升到 `5000000`
- `tokensLimit` 已提升到 `100000000`
- `endDate` 已按 `annual` 自动延至一年后
- 该验证链路证明提档完成后不会再立即受 `free` 或低档位额度阻断

## 3. 边界确认

- 本轮未验证管理员 quota 查询接口，因为本单范围不包含该能力
- 本轮未验证 usage 重置，因为本单范围不包含该能力
- 本轮未新增内部管理页，因此无页面联调项

## 4. 最终验证结论

- 代码层验证通过
- 构建校验通过
- 真实脚本提档链路验证通过
- 当前主线已具备“指定账号可快速提档并解除额度阻断”的最小可交付能力
