# 20260404 工作总结与后续计划

## 今日完成

- 完成 HelloVibe 品牌与发布配置收口：本地 Expo/EAS 配置、Bundle ID、scheme、AASA 关联信息已对齐本次 iOS 发布目标。
- 完成 iOS 发版主链路实操：使用原 Apple ID / 原 Expo 账号体系，跑通 `eas credentials` → `eas build -p ios --profile production` → `eas submit -p ios --profile production`。
- 成功在 Apple Developer 创建并使用 `com.hellovibe.app`，在 App Store Connect 创建 HelloVibe App，并完成 iOS 构建上传。
- TestFlight 已出现构建版本 `1.6.2 (1)`，当前可进入内部测试 / 外部测试分发阶段。
- 明确测试分发规则：内部测试仅限团队成员；朋友、外部联系人更适合走外部测试。

## 本次关键配置调整

- `packages/happy-app/app.config.js`
  - 为兼容现有 EAS 远端项目，保留 Expo slug 为 `happy`
  - 保持正式品牌名 `HelloVibe`
  - 保持正式包名 `com.hellovibe.app`
  - 保持正式 scheme `hellovibe`
- `packages/happy-app/eas.json`
  - 补充 iOS submit 的 `ascAppId = 6761639738`
  - 避免后续提交目标 App 选错
- `packages/happy-app/public/.well-known/apple-app-site-association`
  - 补充当前 Apple Team `SJ7NF8JABX.com.hellovibe.app`
  - 为 Universal Links 做收口

## 已完成验证

- `yarn workspace happy-app typecheck` 通过。
- 生产环境配置解析确认无误：
  - `name = HelloVibe`
  - `slug = happy`
  - `ios.bundleIdentifier = com.hellovibe.app`
  - `android.package = com.hellovibe.app`
- TestFlight 已能看到本次上传构建：`1.6.2 (1)`。

## 关键结论

- 这次 iOS 发布链路已经跑通，后续同类发布可以按同一路径复用。
- Expo/EAS 远端项目当前仍绑定历史 slug `happy`，短期内不应再改成本地 `hellovibe`，否则会再次触发 projectId / slug 不匹配。
- 对外品牌已经切到 HelloVibe，但云端 EAS 项目标识仍是历史名称，这属于可接受的过渡态，不阻塞发版。
- 内部测试只适合团队成员；朋友或外部联系人更适合走外部测试。

## 后续计划

- 先在 TestFlight 安装并完成一轮自测：
  - 启动是否正常
  - 登录是否正常
  - 基础聊天 / 会话是否正常
  - 恢复 / 扫码链路是否正常
  - 深链是否正常
  - 订阅 / 支付相关入口至少做一轮冒烟
- 若自测通过，再决定分发路径：
  - 团队成员走内部测试
  - 朋友 / 外部联系人走外部测试
- 待测试确认无阻塞问题后，把本轮 3 个发布配置文件整理为一次提交，作为 HelloVibe iOS 首轮 TestFlight 基线。

## 今日补充完成

- Ghostty 轻量脚本方向完成收口：不再继续沿“原位重启”思路迭代，已把脚本实际行为改成“新开一个 Ghostty 三分屏窗口”，用于轻量拉起 server + Metro，而不是做破坏性重置。
- 轻量脚本的行为边界已经明确：
  - 会杀掉 `happy-server dev` / `expo start` 和相关端口
  - 会自动拉起 Docker 并等待可用
  - 会新开三分屏窗口：左侧 server、右上 Metro、右下操作区
  - 不会停止 daemon
  - 不会删除 `~/.happy` 或 `~/.happy-rebind-20260327`
  - 不会删除 `Machine` 表
  - 不会自动执行 `auth login --force`
- 已同步修正文档，避免继续把该脚本理解成“原位重启脚本”：
  - `董/测试脚本/20260404-Ghostty现有分屏原位重启-server和Metro.sh`
  - `董/测试脚本/20260404-测试脚本说明-应用关系与使用方式.md`
- 轻量脚本中间修过一次 AppleScript 参数传递问题，现已重新校正并完成基础校验：
  - `bash -n` 通过
  - diagnostics 为空
- 执行端“npm 安装化 + 品牌化命名”需求已完成方案收口，但本轮先不落代码：
  - 已确认后续应先做安装形态收口，再做品牌化命名，再处理本地状态目录迁移
  - 已确认内部目录名 `packages/happy-cli` 暂时不必优先修改
  - 已确认若后续正式对外卖、持续交付、沉淀品牌，应优先统一对外包名、命令名、帮助文案与仓库展示

## 下一步我们要做的

- 先完成 HelloVibe iOS TestFlight 自测与分发判断，确认这一轮发版链路真正可用。
- 进入执行端交付需求的第一阶段：验证 `npm pack` / `npm install -g ./xxx.tgz` / 私有源安装是否能稳定安装执行端，并确认 `auth`、`daemon`、`tmux/Open in Mac` 能正常工作。
- 在安装形态跑通后，再做品牌化的最小改动清单并逐步实施：
  - npm 包名
  - CLI 命令名
  - help / version / 日志文案
  - 仓库展示与对外链接
- 品牌化第一阶段先不急着改内部目录名，也先不直接迁移 `~/.happy`，避免把当前稳定链路一起打断。
- 等安装形态与对外命名都收口后，再单独评估深层兼容项：
  - `~/.happy` / `~/.happy-dev` 状态目录迁移
  - doctor 对新旧命令名的进程识别
  - 旧命令兼容与发布升级策略

## 执行端安装形态验证进展

- 已修复 `happy --version` 的异常行为：
  - 之前在全新 `HAPPY_HOME_DIR` 下执行 `happy --version`，打印版本后还会继续进入主流程，随后触发认证。
  - 现已调整为打印版本后立即退出，不再继续进入 `authAndSetupMachineIfNeeded()`。
- 已重新完成一轮本地安装形态验证：
  - `yarn workspace happy-coder typecheck` 通过。
  - `yarn workspace happy-coder test` 已执行，但当前仓库里本身存在 2 个历史失败用例，本轮改动未新增失败：
    - `packages/happy-cli/src/api/apiSession.test.ts`
    - `packages/happy-cli/src/agent/acp/runAcp.test.ts`
  - `npm pack` 成功，产出 `happy-coder-0.14.0-0.tgz`。
  - `npm install -g ./happy-coder-0.14.0-0.tgz --prefix /tmp/happy-prefix-verify` 成功。
  - 安装后 `happy` / `happy-mcp` 可执行命令已成功暴露。
  - `postinstall` 已完成工具解压，安装目录下可见 `tools/unpacked/difft`、`tools/unpacked/rg`、`tools/unpacked/ripgrep.node`。
- 已在干净环境 `HAPPY_HOME_DIR=/tmp/happy-data-verify` 下验证基础命令：
  - `happy --version`：现在只打印版本并立即退出，不再弹登录。
  - `happy --help`：可正常输出帮助信息。
  - `happy auth status`：可正常输出未登录状态，不会主动拉起认证。
  - `happy doctor`：可正常运行，并能识别安装目录、home 目录、日志目录与未登录状态。
  - `happy daemon start`：现在会在未登录环境下直接失败并提示先执行 `happy auth login`，不再卡进交互认证。
- 已补充 CLI 级回归测试：
  - 新增 `packages/happy-cli/src/index.test.ts`
  - 已验证 `--version` 不再触发认证
  - 已验证 `daemon start` 在未登录环境下会快速失败而不是挂住
- 已补做一轮“已登录隔离环境”验证：
  - 复用 `~/.happy-rebind-20260327` 中的 `access.key` 与 `settings.json` 到临时目录 `/tmp/happy-data-auth-verify`
  - 在安装包前缀 `/tmp/happy-prefix-auth-verify` 下重新安装 tgz 后，`happy auth status` 可识别为已登录
  - `happy daemon start` 成功
  - `happy daemon status` 成功，能看到 PID / HTTP Port / daemon.state.json
  - `happy daemon list` 成功
  - 通过 daemon 本地控制面成功执行一次 `/spawn-session`
  - 生成的会话可被 `daemon list` 正确列出，并可通过 `daemon stop-session` 停止
  - `happy daemon stop` 成功，停止后 `happy daemon status` 能正确回到未运行状态
- 已确认本轮“已登录隔离环境”里 daemon 核心链路是通的：
  - 安装包自身启动的 daemon 不再依赖源码运行
  - daemon 本地控制面 `list / spawn / stop-session / stop` 可正常工作
  - daemon 产出的 session metadata 中 `happyLibDir` / `happyToolsDir` 已指向安装目录，而不是源码目录
- 已补做一轮 Open in Mac / tmux hosted 链路验证：
  - 先通过数据库 + API 解密确认当前账号下存在可恢复的真实 Claude 会话，其中 `cmnk9vzda00r38on6v5pfpuw5` 对应：
    - `claudeSessionId = 16e8fe61-41b8-4bb8-8e2c-99fa67452d5e`
    - `terminalCarrier = fallback`
    - `tmuxSessionId = null`
  - 再直接调用服务端 `GET /v3/sessions/:id/control-state`，确认该会话当前 `controller = mobile`、`leaseVersion = 0`
  - 用真实 Open in Mac 载荷触发 `POST /v3/sessions/:id/handoff/mac` 时，当前服务端返回：
    - `500 handoff-internal-error`
    - `message = Invalid key length`
  - 进一步改用“用户侧 websocket machineRPC”直连后，成功把同一会话重新拉起为 tmux hosted：
    - RPC 方法：`9c65d0e8-6f6d-4b5a-9dae-a4683ec9900e:spawn-happy-session`
    - 参数：`openTerminal = true`、`terminalCarrierMode = hosted`
    - daemon 返回 `type = success`
  - 成功后再次读取会话 metadata，确认已经切到 hosted/tmux 形态：
    - `terminalCarrier = tmux`
    - `tmuxSessionId = 0:happy-1775307773180-claude`
    - `hostPid = 66469`
  - 直接读取 tmux pane 内容，确认安装包版 Claude 已在 tmux window 中启动，并进入目录信任确认页，而不是退回源码路径或 fallback 进程。
  - 验证结束后，已执行 `daemon stop-session` 与 `daemon stop` 清理现场，tmux window 也已消失。
- 当前新发现的安装态风险：
  - `happy daemon start` 虽然已经不会再卡进交互认证，但在“未登录环境”下仍然只能验证到“快速失败且给出明确提示”，还不能覆盖 daemon 真正启动成功的链路。
  - 安装包版 daemon 自身的 hosted/tmux 链路已经验证通过；随后继续往下查，已把服务端 `/v3/sessions/:id/handoff/mac` 的 key 使用问题定位并完成代码修复：
    - `Machine.dataEncryptionKey` 在库里存的是加密 bundle，长度约 105 bytes，并不是裸 32-byte key
    - 服务端原来把这个 bundle 直接传给 `callConnectionRpcWithVariantRetry(...)`，所以真实正式入口才会报 `Invalid key length`
    - 当前修复方案不是“强行在服务端解 bundle”，而是：
      - 如果拿到的是 32-byte key，仍走原有加密 RPC
      - 如果拿到的是 bundle，则改走新增的 plaintext RPC bridge，由 daemon 本地直接执行 `spawn-happy-session`
    - 这样做的原因是：按当前架构，服务端并不掌握可直接把 bundle 解回原始 machine key 的用户私钥材料
  - 当前仍剩一个收口动作：
    - 代码级修复和针对性测试已经通过
    - 但还差一次“真实已登录隔离环境下的正式 `/handoff/mac` 入口回归”，用来确认线上同形态调用不再报 `Invalid key length`
  - 当前 tarball 仍然偏重：
    - package size 约 `121.9 MB`
    - unpacked size 约 `238.4 MB`
    - 原因是打包中同时包含 `tools/archives/*` 与 `tools/unpacked/*`
- 下一轮安装验证应拆成两段：
  - 无登录环境：只验证 `--help`、`--version`、`auth status`、`doctor`、安装目录与工具解压。
  - 已登录环境：`daemon start/status`、tmux hosted 真实运行链路已验证；服务端 `/handoff/mac` 已完成代码修复与测试，下一步改为做正式入口真回归。

## 执行端安装形态验证 checklist

- [x] 确认执行端当前实际打包对象
  - [x] 核对 `packages/happy-cli/package.json`
  - [x] 明确当前 npm 包名、bin 命令名、`files`、`postinstall`、平台工具目录
- [x] 验证本地可产出可安装包
  - [x] 执行 `yarn workspace happy-coder build`
  - [x] 执行 `npm pack` 或等价打包命令
  - [x] 确认产物 `.tgz` 内包含 `dist / bin / scripts / tools`
- [x] 验证 tgz 安装链路
  - [x] 在干净目录或干净环境执行 `npm install -g ./xxx.tgz`
  - [x] 确认可执行命令已成功暴露
  - [x] 确认 `postinstall` 已完成工具解压
- [~] 验证基础命令可用性
  - [x] `--help`
  - [x] `--version`
  - [x] `auth`
  - [x] `daemon start`
  - [x] `daemon status`
  - [x] `doctor`
- [~] 验证执行端核心链路不回退
  - [x] 本地状态目录可正常创建和读取
  - [x] daemon 可正常拉起、识别版本、写入状态
  - [x] daemon hosted / tmux 托管能力未因安装形态失效
  - [~] 服务端 `/v3/sessions/:id/handoff/mac` 已完成代码修复与针对性测试，还差正式入口真回归
- [x] 记录安装形态风险项
  - [x] 是否仍隐式依赖源码仓库路径
  - [x] 是否仍隐式依赖开发命令或 workspace 结构
  - [x] 是否存在平台工具权限、路径、解压失败问题
- [ ] 安装形态通过后，再进入品牌化最小改动设计
  - [ ] 包名改造
  - [ ] 命令名改造
  - [ ] help/version/日志文案改造
  - [ ] 对外仓库链接与展示改造

## 今日继续完成（服务端 handoff/mac 修复）

- 已完成服务端 `/v3/sessions/:id/handoff/mac` 的进一步排查与修复，不再停留在“知道报错原因”，而是已经真正改了代码。
- 这次修复的核心判断已经明确：
  - 问题不在 daemon hosted/tmux 本身
  - 问题在服务端正式入口错误使用了 `Machine.dataEncryptionKey`
  - 库里保存的是加密 bundle，不是可直接用于 AES-256-GCM / secretbox 的 32-byte 原始 key
- 服务端本轮改动：
  - `packages/happy-server/sources/app/api/routes/v3SessionRoutes.ts`
  - 新增 plaintext RPC fallback
  - 当 `key.length !== 32` 时，不再继续做加密 RPC，而是直接走 `rpc-request-plaintext`
  - 仍保留原有加密 RPC 逻辑，用于兼容 raw key 场景
- daemon 配套改动：
  - `packages/happy-cli/src/api/rpc/RpcHandlerManager.ts`
    - 抽出公共 handler 调用逻辑
    - 新增 `handlePlaintextRequest(...)`
  - `packages/happy-cli/src/api/apiMachine.ts`
    - machine-scoped websocket 新增 `rpc-request-plaintext` 事件监听
    - 收到明文请求后，直接复用已有 `spawn-happy-session` handler 执行业务逻辑
- 本轮新增回归测试：
  - `packages/happy-server/sources/app/api/routes/v3SessionRoutes.test.ts`
  - 已覆盖两种场景：
    - raw 32-byte machine key 场景
    - 真实 encrypted machine key bundle 场景
  - 第二种场景已明确断言：服务端会走 `rpc-request-plaintext`
- 本轮已完成验证结果：
  - `GetDiagnostics` 为空
  - `yarn workspace happy-coder typecheck` 通过
  - `yarn workspace happy-server test sources/app/api/routes/v3SessionRoutes.test.ts -t "Open in Mac handoff"` 通过
    - raw key handoff 用例通过
    - encrypted bundle handoff 用例通过
  - 整个 `v3SessionRoutes.test.ts` 目前仍有 7 个 message 相关旧用例失败，但这些失败集中在 message send / quota 路径，不是本轮 handoff/mac 修复直接引入的问题
  - `yarn workspace happy-server build` 仍失败，失败点集中在已有 quota 代码：
    - `tokenQuota.ts`
    - `quotaRoutes.ts`
    - 缺少 Prisma 上的 `subscriptionPlan` / `dailyUsage` 类型
- 因此今天的真实结论是：
  - handoff/mac 的“错误 key 使用”问题，代码上已经修掉
  - 针对这个问题新增的回归保护已经加上
  - 当前还没收口的是“真实正式入口再跑一遍”
  - 另外还暴露出一个独立旧问题：happy-server 的 quota/Prisma 类型本身不一致，需要单独处理

## 明天开工直接按这个顺序做

- 第一优先级：把正式 `/v3/sessions/:id/handoff/mac` 真回归跑完
  - 使用已登录隔离环境继续验证，不要重新引入人工认证打断
  - 先确认 daemon 已在安装包环境里正常启动，machine-scoped 连接在线
  - 再读取目标 session 的 `control-state`
  - 然后重新调用正式 `POST /v3/sessions/:id/handoff/mac`
  - 重点确认四件事：
    - 不再出现 `Invalid key length`
    - 返回码恢复正常
    - `controller / leaseVersion / handoffState` 更新符合预期
    - 会话 metadata 中 `terminalCarrier`、`tmuxSessionId` 真正切到 tmux/hosted 结果
- 第二优先级：把正式回归结果补齐到日志与结论里
  - 如果正式入口已通过，要在日志里明确写清楚：
    - 哪个 session 做的验证
    - 调用前状态是什么
    - 调用后状态是什么
    - tmux window / pane 是否真正拉起
  - 如果正式入口仍有问题，要明确记录：
    - 卡在哪一步
    - 是 server 返回异常、socket 未连上，还是 daemon handler 侧失败
    - 下一个要改的具体文件与函数
- 第三优先级：处理 happy-server 当前独立阻塞项
  - 先核对 Prisma schema / generate 状态，确认 `subscriptionPlan`、`dailyUsage` 是否应该存在
  - 再判断是：
    - schema 已改但 client 未 regenerate
    - 还是 quota 代码先落地了，但 schema/migration 尚未对齐
  - 目标不是一上来大改，而是先把 build 卡点和 message 路径 500 的真实根因钉死
- 第四优先级：收口“安装形态阶段是否完成”的判断
  - 如果正式 `/handoff/mac` 真回归通过，安装形态第一阶段就基本可以判定收口
  - 收口后再开始下一阶段：品牌化最小改动设计，不要和安装验证混在一起

## 明天具体执行清单

- 先看今天这份日志，不要重新从头猜
- 优先复用今天已经验证过的隔离目录与机器信息：
  - `HAPPY_HOME_DIR=/tmp/happy-data-auth-verify`
  - 安装前缀：`/tmp/happy-prefix-auth-verify`
  - machineId：`9c65d0e8-6f6d-4b5a-9dae-a4683ec9900e`
- 优先复用今天已确认过的真实 session 线索：
  - `sessionId = cmnk9vzda00r38on6v5pfpuw5`
  - `claudeSessionId = 16e8fe61-41b8-4bb8-8e2c-99fa67452d5e`
- 明天开工后的推荐顺序：
  - 先确认 daemon 状态
  - 再查 `control-state`
  - 再打 `/handoff/mac`
  - 再查 metadata / tmux
  - 最后再决定是否进入 quota 问题排查
- 明天不要先做的事：
  - 不要先碰品牌化命名
  - 不要先改内部目录名
  - 不要先做 `~/.happy` 状态迁移
  - 不要在 quota 问题没定位前顺手大面积改 server 其它路由

## 明天第一步执行手册（命令版）

### 0）先统一本次验证使用的固定信息

- 本次仍以“已登录隔离环境 + 安装包版 happy + 真实 session 正式入口回归”为目标，不要临时切回源码直跑 happy。
- 固定信息如下：
  - `HAPPY_HOME_DIR=/tmp/happy-data-auth-verify`
  - 安装前缀：`/tmp/happy-prefix-auth-verify`
  - 可执行文件：`/tmp/happy-prefix-auth-verify/bin/happy`
  - machineId：`9c65d0e8-6f6d-4b5a-9dae-a4683ec9900e`
  - happy sessionId：`cmnk9vzda00r38on6v5pfpuw5`
  - claudeSessionId：`16e8fe61-41b8-4bb8-8e2c-99fa67452d5e`
- 如果这些值被新的真实环境替换了，必须先把新值补回本日志，再继续后面的步骤。

### 1）先起好环境，不要一上来就直接打接口

先在终端执行：

```bash
export HAPPY_HOME_DIR=/tmp/happy-data-auth-verify
export HAPPY_BIN=/tmp/happy-prefix-auth-verify/bin/happy
export HAPPY_SERVER_URL=http://127.0.0.1:3005
```

检查点：
- `HAPPY_HOME_DIR` 目录里应当已有 `access.key` 和 `settings.json`
- `settings.json` 里的 `machineId` 应当还是 `9c65d0e8-6f6d-4b5a-9dae-a4683ec9900e`
- `HAPPY_BIN` 必须指向安装产物，不要误用源码里的 `tsx src/index.ts`

### 2）确认 server 进程是否在线

如果本地 server 没起，先在仓库根目录启动：

```bash
yarn workspace happy-server dev
```

检查点：
- server 能正常监听 `3005`
- 启动日志里不要出现新的启动级报错
- 如果 server 启不来，先不要继续 handoff 验证，先把 server 启动失败原因记下来

### 3）确认安装包版 daemon 处于可用状态

先看登录态和 daemon 状态：

```bash
"$HAPPY_BIN" auth status
"$HAPPY_BIN" daemon status
```

如果 daemon 未启动，再执行：

```bash
"$HAPPY_BIN" daemon start
"$HAPPY_BIN" daemon status
"$HAPPY_BIN" daemon list
```

检查点：
- `auth status` 必须显示已登录，不能回到要求重新扫码认证
- `daemon start` 不能报未登录错误
- `daemon status` 要能看到 PID、HTTP Port、状态文件
- `daemon list` 至少要能正常返回，不应报 daemon 不可达

### 4）确认 machine-scoped 连接是否已经回到服务端

这一条的目的不是看业务结果，而是先确认 daemon 真正连上 server 了。

建议先看 daemon 日志目录：

```bash
ls -lt /tmp/happy-data-auth-verify/logs | head
ls -lt /tmp/happy-data-auth-verify/logs/open-in-mac | head
```

检查点：
- 有新的 daemon 日志文件产生
- 日志里如果能看到 machine 连接成功、keep-alive、RPC register 相关信息更好
- 如果 daemon 已启动但 machine-scoped socket 没连上 server，就不要急着打 `/handoff/mac`

### 5）先读正式入口当前状态，不要盲打 handoff

这里要先准备一个有效的用户 Bearer token，但不要把真实 token 写进日志。

先在当前 shell 手动放入：

```bash
export HAPPY_BEARER_TOKEN='<USER_BEARER_TOKEN>'
export HAPPY_SESSION_ID=cmnk9vzda00r38on6v5pfpuw5
export HAPPY_MACHINE_ID=9c65d0e8-6f6d-4b5a-9dae-a4683ec9900e
export HAPPY_CLAUDE_SESSION_ID=16e8fe61-41b8-4bb8-8e2c-99fa67452d5e
```

然后查询 control-state：

```bash
curl -sS \
  -H "Authorization: Bearer $HAPPY_BEARER_TOKEN" \
  "$HAPPY_SERVER_URL/v3/sessions/$HAPPY_SESSION_ID/control-state"
```

检查点：
- 必须先拿到当前 `controller`、`leaseVersion`、`handoffState`
- 如果这里已经不是预期的 mobile / idle 状态，要先记下真实返回，不要按旧值硬打
- 后面调用 `/handoff/mac` 时，`expectedLeaseVersion` 必须使用这里刚查到的最新值

### 6）正式回归 `/handoff/mac`

把上一步查到的 leaseVersion 填进去，再执行：

```bash
curl -sS \
  -X POST \
  -H "Authorization: Bearer $HAPPY_BEARER_TOKEN" \
  -H "Content-Type: application/json" \
  "$HAPPY_SERVER_URL/v3/sessions/$HAPPY_SESSION_ID/handoff/mac" \
  -d '{
    "expectedLeaseVersion": <LATEST_LEASE_VERSION>,
    "machineId": "'"$HAPPY_MACHINE_ID"'",
    "directory": "/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/happy-main",
    "claudeSessionId": "'"$HAPPY_CLAUDE_SESSION_ID"'",
    "tmuxSessionId": "happy:window-1",
    "openTerminal": true,
    "terminalCarrierMode": "hosted"
  }'
```

检查点：
- 第一件事：不能再出现 `Invalid key length`
- 第二件事：返回不能再是 `500 handoff-internal-error`
- 第三件事：如果成功，返回体里应能看到 `success = true`
- 第四件事：`state.controller / state.leaseVersion / state.handoffState` 要符合 handoff 成功后的预期

### 7）成功后立刻做二次确认，不要只看一个 200 就结束

再次查询：

```bash
curl -sS \
  -H "Authorization: Bearer $HAPPY_BEARER_TOKEN" \
  "$HAPPY_SERVER_URL/v3/sessions/$HAPPY_SESSION_ID/control-state"
```

然后检查 tmux：

```bash
tmux ls
tmux list-windows -a | grep happy || true
```

必要时再看 open-in-mac 日志：

```bash
ls -lt /tmp/happy-data-auth-verify/logs/open-in-mac | head
```

检查点：
- `controller`、`leaseVersion`、`handoffState` 必须和 handoff 返回结果对得上
- tmux 里应当真的出现对应窗口，而不是只有接口返回成功
- open-in-mac 日志里不应再看到旧的 `Invalid key length`
- 如果 tmux 没起来，但接口返回成功，说明问题已经从 server key 使用，转移到了 daemon handler 或本地启动阶段，要分开记

### 8）如果失败，按这个顺序判断，不要乱改

- 如果 `control-state` 就拿不到：
  - 先查 token、server、sessionId 是否对
- 如果 `/handoff/mac` 还是 500：
  - 先记完整返回体
  - 再看 happy-server 日志
  - 再看 daemon/open-in-mac 日志
- 如果接口成功但 tmux 没起来：
  - 重点查 daemon 收到的 `rpc-request-plaintext` 是否执行到了 `spawn-happy-session`
- 如果 daemon 没连上 server：
  - 先解决 machine-scoped socket 在线问题，不要提前改 handoff 逻辑

### 9）做完后必须补记这 6 件事

- 当时使用的：
  - `sessionId`
  - `claudeSessionId`
  - `machineId`
  - `expectedLeaseVersion`
- `control-state` 调用前的结果
- `/handoff/mac` 的完整返回摘要
- 调用后的 `control-state`
- tmux 是否真正拉起
- 最终结论到底是哪一种：
  - 正式入口已打通
  - server 已通但 daemon 本地还卡住
  - 仍有新的 server 侧错误待修

## 今晚补充实跑结果（正式 `/handoff/mac` 已回归通过）

- 已按上面的执行手册，实际把正式入口重新跑了一遍，不再只是停留在“代码已改 + 测试已过”。
- 本次实跑前，先补做了两个必要动作：
  - 重新 `yarn build`
  - 重新 `npm pack` 并把最新 tgz 安装回 `/tmp/happy-prefix-auth-verify`
- 这样做的原因是：
  - 前面虽然源码里的 server/daemon 都已经改过
  - 但隔离验证环境里真正执行的还是安装包版 `happy`
  - 如果不重新打包安装，安装包里的 daemon 仍然是旧代码，正式回归会继续拿旧行为
- 本次正式回归使用的固定信息：
  - `HAPPY_HOME_DIR=/tmp/happy-data-auth-verify`
  - 安装包命令：`/tmp/happy-prefix-auth-verify/bin/happy`
  - `machineId = 9c65d0e8-6f6d-4b5a-9dae-a4683ec9900e`
  - `sessionId = cmnk9vzda00r38on6v5pfpuw5`
  - `claudeSessionId = 16e8fe61-41b8-4bb8-8e2c-99fa67452d5e`
- 调用前 `control-state`：
  - `controller = mobile`
  - `leaseVersion = 0`
  - `handoffState = failed`
  - `handoffReason = resume-failed:operation has timed out`
- 本次正式调用结果：
  - `POST /v3/sessions/:id/handoff/mac` 返回 `200`
  - 返回体里 `success = true`
  - `resumedHappySessionId = cmnk9vzda00r38on6v5pfpuw5`
  - 返回 state：
    - `controller = mac`
    - `leaseVersion = 1`
    - `handoffState = idle`
    - `handoffReason = null`
- 调用后再次读取 `control-state`，结果与 handoff 返回一致：
  - `controller = mac`
  - `leaseVersion = 1`
  - `handoffState = idle`
- daemon 侧实际观测结果：
  - 最新 daemon 日志里可以看到 `Received plaintext RPC request`
  - 方法名是：`9c65d0e8-6f6d-4b5a-9dae-a4683ec9900e:spawn-happy-session`
  - 传入参数里已经带上：
    - `tmuxSessionId = happy:window-1`
    - `terminalCarrierMode = hosted`
  - daemon 日志继续显示：
    - `[RPC] Calling handler`
    - `[API MACHINE] Spawning session with params ...`
    - `[API MACHINE] Spawned session cmnk9vzda00r38on6v5pfpuw5`
    - `[RPC] Handler returned`
- tmux 侧检查结果：
  - `tmux ls` 可见 tmux session 在线
  - `tmux list-windows -a | grep happy` 可见 `happy` 窗口仍在
  - 结合 handoff 返回成功、control-state 已切到 `mac`、daemon 日志明确执行到 `spawn-happy-session`，本轮可以判定正式入口已经打通
- 这次实跑得到的关键结论：
  - 之前真实正式入口的 `Invalid key length` 已不再出现
  - `/v3/sessions/:id/handoff/mac` 现在已经可以在已登录隔离安装环境里正常完成 handoff
  - 安装形态第一阶段里，Open in Mac 正式入口这条高优先级链路可以视为收口
- 这次顺手确认到的一个实践点：
  - 这种“源码已改，但验证环境跑的是 tgz 安装包”的场景里，每次改到 daemon / websocket / RPC 行为后，都要重新打包并安装回隔离前缀，否则很容易误判成“服务端没修好”

## 下一步优先级（已调整）

- `/handoff/mac` 正式入口回归已完成，下一步不再把它当第一阻塞项。
- 当前剩余更像独立阻塞项的是：
  - `happy-server` 的 quota / Prisma 类型不一致
  - `v3SessionRoutes.test.ts` 里 message 路径仍有 7 个旧失败用例
  - `subscriptionPlan` / `dailyUsage` 相关 build 卡点
- 因此下一轮建议顺序改为：
  - 先定位 quota / Prisma 类型问题到底是 schema、generate 还是代码先行不一致
  - 再收口 message 路径为什么统一返回 500
  - 安装形态阶段确认收口后，再进入品牌化最小改动设计
