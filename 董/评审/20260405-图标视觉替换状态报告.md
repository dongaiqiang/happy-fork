# 20260405 图标视觉替换状态报告

**创建时间：** 2026-04-05  
**状态：** 进行中（移动端已完成，桌面端待确认）  
**采用方案：** concept-a（第一稿）

---

## 1. 当前状态

### 1.1 已完成（2026-04-03 17:24 更新）

| 文件 | 状态 | 路径 |
|------|------|------|
| `icon.png` | ✅ 已替换 | `packages/happy-app/sources/assets/images/icon.png` |
| `icon-adaptive.png` | ✅ 已替换 | `packages/happy-app/sources/assets/images/icon-adaptive.png` |
| `icon-monochrome.png` | ✅ 已替换 | `packages/happy-app/sources/assets/images/icon-monochrome.png` |
| `icon-notification.png` | ✅ 已替换 | `packages/happy-app/sources/assets/images/icon-notification.png` |
| `splash-android-light.png` | ✅ 已替换 | `packages/happy-app/sources/assets/images/splash-android-light.png` |
| `splash-android-dark.png` | ✅ 已替换 | `packages/happy-app/sources/assets/images/splash-android-dark.png` |
| `favicon.png` | ✅ 已替换 | `packages/happy-app/sources/assets/images/favicon.png` |

### 1.2 待确认/待完成

| 文件 | 状态 | 路径 | 说明 |
|------|------|------|------|
| `icon-tauri.png` | ⚠️ 待确认 | `packages/happy-app/src-tauri/icons/` | 桌面端图标，最后修改时间 Feb 25 |

---

## 2. 源文件位置

所有 concept-a SVG 源文件位于：
```
董/图片/
├── helloVibe-icon-concept-a.svg
├── helloVibe-icon-concept-a-adaptive-foreground.svg
├── helloVibe-icon-concept-a-monochrome.svg
├── helloVibe-icon-concept-a-notification.svg
├── helloVibe-splash-concept-a-light.svg
└── helloVibe-splash-concept-a-dark.svg
```

---

## 3. 验收清单

### 3.1 移动端（Expo）

- [x] iOS 主图标 (`icon.png`)
- [x] Android 自适应图标 (`icon-adaptive.png`)
- [x] Android 单色图标 (`icon-monochrome.png`)
- [x] 通知图标 (`icon-notification.png`)
- [x] Android 启动图 (`splash-android-light.png` / `splash-android-dark.png`)
- [x] Web favicon (`favicon.png`)

### 3.2 桌面端（Tauri）

- [ ] macOS 图标 (`src-tauri/icons/icon.icns` / `.png`)
- [ ] Windows 图标 (`src-tauri/icons/icon.ico`)
- [ ] Linux 图标 (`src-tauri/icons/icon.png`)

---

## 4. 待确认事项

### 4.1 Tauri 图标状态

需要确认：
1. `packages/happy-app/src-tauri/` 目录结构
2. Tauri 图标是否已更新为 HelloVibe concept-a
3. 如未更新，需要从 SVG 重新生成

### 4.2 验证步骤

需要验证：
1. iOS 真机/模拟器检查桌面图标
2. Android 真机/模拟器检查桌面图标、通知图标、启动图
3. Web 端检查 favicon
4. Tauri 桌面应用检查图标

---

## 5. 下一步行动

### 方案 A：确认 Tauri 图标状态

```bash
# 1. 检查 Tauri 图标目录
ls -la packages/happy-app/src-tauri/icons/

# 2. 检查 tauri.conf.json 中的图标配置
cat packages/happy-app/src-tauri/tauri.conf.json | grep -A 5 "icon"
```

### 方案 B：生成 Tauri 图标（如需要）

如果 Tauri 图标未更新，需要：
1. 从 concept-a SVG 生成多尺寸 PNG
2. 使用 `tauri icon` 命令生成 `.icns` / `.ico`
3. 更新 `tauri.conf.json` 中的图标引用

---

## 6. 总体判断

**移动端图标替换：已完成 90%+**
- 7 个核心资源已更新（Apr 3）
- 已在 `app.config.js` 中正确引用

**桌面端图标替换：待确认**
- `icon-tauri.png` 仍为 Feb 25 旧文件
- 需要检查 `src-tauri/` 目录

**建议：** 先确认 Tauri 图标状态，再决定是否需要在发布前更新。

---

## 7. 相关文件

- `packages/happy-app/app.config.js` — Expo 配置
- `packages/happy-app/sources/assets/images/` — 移动端图标
- `packages/happy-app/src-tauri/tauri.conf.json` — Tauri 配置
- `packages/happy-app/src-tauri/icons/` — Tauri 图标源文件
- `董/图片/` — concept-a SVG 源文件
