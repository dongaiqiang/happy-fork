# 20260407-tarball体积与包内容边界结果

- 工作目录：`/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/hellovibe-main`
- 目标：承接 Claude 的 tgz / tarball 审计结论，把“问题判断”收成“可交付边界”

## 一、承接的旧判断

### 1.1 继续成立

- `/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/hellovibe-claude/董/claudework/20260405-并行支持任务交付报告.md`
  - `tools/archives/*.tar.gz` 是包体积过大的主因
  - `postinstall` 本身不是问题，问题在于打包边界过宽
- `/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/happy-main/董/评审/20260405-包内容瘦身方案.md`
  - 应优先收紧包边界，而不是把 tarball 持续做成“全平台二进制合集”
- `/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/happy-main/董/评审/20260405-评审吸收版优先级清单.md`
  - 先做最短闭环，不先引入更大的下载分发体系

### 1.2 本轮覆盖后的最新结论

- 本轮未采用“立刻改成 GitHub Releases 在线下载”的大方案
- 本轮采用的是更短闭环的执行边界：
  - 保留运行时需要的脚本
  - 保留 macOS 两套 archive
  - 去掉与当前外部交付无关的 Linux / Windows 归档

## 二、实际边界调整

### 2.1 修改前

- `package.json.files` 过宽：
  - `bin`
  - `scripts`
  - `tools`
- 结果：
  - tarball 会把 `tools/archives/` 下全部平台归档都打进去
  - 还会把一批不参与对外交付的开发脚本一并打进包

### 2.2 修改后

- `/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/hellovibe-main/packages/happy-cli/package.json`
  - 改为显式白名单：
    - `dist`
    - `bin/happy.mjs`
    - `bin/happy-mcp.mjs`
    - `scripts/claude_local_launcher.cjs`
    - `scripts/claude_remote_launcher.cjs`
    - `scripts/claude_version_utils.cjs`
    - `scripts/ripgrep_launcher.cjs`
    - `scripts/session_hook_forwarder.cjs`
    - `scripts/unpack-tools.cjs`
    - `tools/archives/difftastic-arm64-darwin.tar.gz`
    - `tools/archives/difftastic-x64-darwin.tar.gz`
    - `tools/archives/ripgrep-arm64-darwin.tar.gz`
    - `tools/archives/ripgrep-x64-darwin.tar.gz`
    - `tools/licenses`
    - `package.json`
- `/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/hellovibe-main/packages/happy-cli/.npmignore`
  - 继续排除 `tools/unpacked/`
  - 明确排除测试文件
- `/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/hellovibe-main/packages/happy-cli/scripts/unpack-tools.cjs`
  - 当前平台缺 archive 时，错误文案会直接说明 tgz 内真实带了哪些平台

## 三、修改前后对比

| 指标 | 修改前 | 修改后 | 变化 |
| --- | ---: | ---: | ---: |
| package size | 105213640 B | 30870668 B | -74342972 B |
| unpacked size | 112293882 B | 34008114 B | -78285768 B |
| total files | 66 | 50 | -16 |

### 3.1 变化解释

- 主要节省来自剔除 Linux / Windows 的 difftastic 与 ripgrep archive
- 次要节省来自不再把整个 `scripts/` 和整个 `bin/` 全量打包
- 安装后仍能通过 `postinstall` 解出 macOS 需要的 `difft`、`rg`、`ripgrep.node`

## 四、当前对外交付边界

### 4.1 已明确可交付

- 当前 tarball 已按 macOS 交付最小闭环收口
- 已真实验证：
  - `npm pack`
  - 干净前缀 `npm install -g`
  - `happy --version`
  - `happy --help`
  - `happy auth status`
  - `happy auth login` 入口
  - `happy daemon status`
  - `happy daemon start` 的未登录态报错
  - tmux / hosted 探针

### 4.2 本轮刻意没有继续扩的部分

- 没有引入在线下载 archive 的分发体系
- 没有把 tarball 改成全平台统一对外交付
- 没有改 CLI 名称、安装命令、升级命令或打包产物名

## 五、当前判断

- 这轮结果已经把 tarball 从“能讨论”推进到“能真实交付并说明边界”
- 对外口径应明确为：
  - 当前 tgz 真安装闭环已验证
  - 当前 tarball 边界已收紧到 macOS 交付所需最小集合
  - 如果下一步要做 Linux / Windows 的同级交付，应单独设计 archive 分发策略，而不是再把所有平台重新塞回一个 tgz
