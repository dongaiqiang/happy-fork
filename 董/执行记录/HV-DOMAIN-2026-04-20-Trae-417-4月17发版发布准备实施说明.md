# HV-DOMAIN-2026-04-20-Trae-417-4月17发版发布准备实施说明

## 一、最小补改范围
本轮只处理“`417` 发布准备与阿里云上线前验证”直接相关内容，不混入 `user-first`、后台原型、附件上传或其他并行主题。

实际补改文件：
- `/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/hellovibe-417/packages/happy-app/sources/utils/sessionControlUtils.ts`
- `/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/hellovibe-417/packages/happy-app/sources/utils/sessionControlUtils.test.ts`
- `/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/hellovibe-417/packages/happy-app/sources/agents/catalog.ts`
- `/Users/dongaiqiang/Documents/mycode/codes/00-chanpin/hellovibe-417/packages/happy-app/sources/agents/catalog.test.ts`

## 二、为什么补这四个文件
- 首次 production Web 导出失败，不是因为域名配置错误，而是 `happy-app` 存在对缺失模块的直接引用，导致无法生成部署产物。
- 这两个模块在 `hellovibe-main` 已有现成实现与测试；`hellovibe-417` 缺失它们，属于发布前必须补齐的依赖缺口。
- 采用“直接补回主线已有最小实现 + 同步测试”的方式，避免在 `417` 线重写逻辑或扩散修改面。

## 三、TDD / 验证驱动过程
1. 先运行 `APP_ENV=production npx expo export --platform web --output-dir dist`，得到失败输入：缺少 `@/utils/sessionControlUtils`。
2. 补齐 `sessionControlUtils.ts` 与测试文件后，先跑 `sources/utils/sessionControlUtils.test.ts`，确认补丁行为可验证。
3. 再次运行 production Web 导出，得到新的失败输入：缺少 `@/agents/catalog`。
4. 同步 `catalog.ts` 与测试文件后，再跑 `sources/agents/catalog.test.ts` 与相关定点测试。
5. 第三次运行 production Web 导出，确认 `dist` 产物真实生成成功。

## 四、为什么当前仍不放行
即使本地导出已经成功，当前仍不满足“可以发布”：
- `https://api.hellovibe-ai.com` 现网仍返回 `502`
- `https://hellovibe-ai.com` 和 `https://app.hellovibe-ai.com` 虽然返回 `200`，但页面标题仍是 `easycode (dev)`，说明线上静态包仍未替换为本轮本地产物
- 当前会话没有服务器侧发布权限，无法在本轮内完成 `/var/www/happy-web` 替换、服务端进程修复与上线后人工验站

## 五、下一步最小动作
- 先把本轮代码与正式记录提交一版，固定发布准备现场。
- 拿着本轮已验证通过的本地 `dist` 产物，在阿里云执行：
  - 替换 `/var/www/happy-web`
  - 修复 `happy-api` / `PUBLIC_URL` / 相关服务端环境
  - 重新校验 `https://api.hellovibe-ai.com/health`
  - 打开 `https://hellovibe-ai.com` 与 `https://app.hellovibe-ai.com`，确认页面标题和实际文案已切到本轮口径
- 上述动作完成前，本轮维持“还不能发布”。
